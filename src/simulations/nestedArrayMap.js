const array = [[0,1,2],[2,3,4]];

array.map(item => {
    return item.map(subitem => console.log(subitem));
});

playerContainer.map(player => {
    return player.map(period => {
        
    })
})